# Ūdens Skaitītāji - Kazarmu 7

Веб-приложение для учета показаний счетчиков воды в многоквартирном доме.

## Возможности

- 🏠 **Админ-панель** - управление жильцами и просмотр всех показаний
- 🔐 **Приватные ссылки** - каждый жилец получает уникальную ссылку
- 💧 **Учет холодной воды** - поддержка 1-5 счетчиков на квартиру
- 📊 **Автоматический расчет** - потребление и стоимость
- 📱 **Адаптивный дизайн** - работает на всех устройствах
- 💾 **Постоянное хранение** - данные сохраняются между сессиями

## Установка

```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd water-meters-app

# Установить зависимости
npm install

# Запустить dev-сервер
npm run dev

# Собрать для продакшена
npm run build
```

## Использование

### Для администратора

1. Откройте приложение без параметров в URL
2. Вы увидите список всех жильцов
3. Нажмите "Kopēt saiti" рядом с квартирой, чтобы скопировать ссылку
4. Отправьте ссылку жильцу (например, по email)

### Для жильцов

1. Откройте персональную ссылку, полученную от администратора
2. Вы увидите только свою квартиру
3. Нажмите "Pievienot rādījumus", чтобы добавить новые показания
4. Введите показания всех счетчиков и сохраните

## Структура проекта

```
water-meters-app/
├── src/
│   ├── components/
│   │   ├── AdminView.jsx       # Админ-панель
│   │   ├── AdminView.css
│   │   ├── ResidentView.jsx    # Интерфейс жильца
│   │   └── ResidentView.css
│   ├── data/
│   │   └── residents.js        # Начальные данные жильцов
│   ├── utils/
│   │   └── storage.js          # Утилиты для работы с хранилищем
│   ├── App.jsx                 # Главный компонент
│   ├── main.jsx               # Точка входа
│   └── index.css              # Глобальные стили
├── index.html
├── package.json
├── vite.config.js
└── README.md
```

## Технологии

- **React 18** - UI библиотека
- **Vite** - сборщик и dev-сервер
- **Lucide React** - иконки
- **Claude Artifacts Storage** - постоянное хранение данных

## Конфигурация

### Тарифы

Тарифы настраиваются в `src/data/residents.js`:

```javascript
export const WATER_RATE = 0.85; // EUR per m³
```

### Начальные жильцы

Список жильцов находится в `src/data/residents.js`. При первом запуске они автоматически добавятся в систему.

## Развертывание

### Netlify / Vercel

1. Подключите репозиторий к платформе
2. Укажите команду сборки: `npm run build`
3. Укажите директорию публикации: `dist`
4. Деплой произойдет автоматически

### GitHub Pages

```bash
npm run build
# Загрузите содержимое папки dist на GitHub Pages
```

## Поддержка браузеров

- Chrome/Edge (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Mobile browsers

## Лицензия

MIT

## Поддержка

При возникновении вопросов создайте Issue в репозитории.
